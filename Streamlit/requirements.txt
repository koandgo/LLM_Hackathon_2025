# requirements.txt
streamlit>=1.34
nbformat>=5.10
nbclient>=0.10
gitpython>=3.1
openai>=1.40
pandas>=2.2
numpy>=1.26
# You may need these if the notebook uses them:
tqdm
requests
python-dateutil
# If the notebook saves HTML or uses nbconvert, uncomment:
# nbconvert>=7.10
